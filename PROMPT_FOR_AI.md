# 切换到本目录后，把下面整段发给 AI 即可

---

你在 `/root/infinstall` 工作。这是产品 **无限安装（Infinstall）** 的工程目录。

请先阅读同目录 `PROJECT.md`，再按我的指示推进。

## 产品共识（勿偏离）

- **中文名：无限安装**（谐音无线安装；强调少限制安装，不是卖调试工具）
- **英文名：Infinstall**；目录名 `infinstall`
- **定位：手机无线给智能电视 / 机顶盒安装与管理应用**（装 APK、列应用、卸载）
- **网络调试只是实现手段**，不进产品名、不进主界面主文案；用户流程是「连电视 → 选包安装 → 管理应用」
- **连接方式**：电视开启网络调试后，App **优先局域网自动发现**设备，其次连接历史，再不行才**手动输入 IP + 端口**（默认 5555）。不是「只能手输」；自动检测能做则尽量做好。
- **不要做、也不要写进方案：二维码扫码连接**。电视开网络调试后不会给连接用二维码。
- **功能一次做完整**：连接（发现/历史/手输）+ 安装（含分享进入、批量）+ 应用管理 + 中文失败说明 + 简要开启引导。不要用「V1 / V2 / 二期再说」把范围砍半。
- **手机 + 平板都要舒服**：同一套 App 自适应。手机单栏；平板宽屏可用双栏/限宽居中，触控大、间距松，横竖屏可用。按你自己在平板上的使用场景优先打磨，不能只按小屏手机凑合。
- **技术**：Kotlin Android App；SAF 选 APK；协议库（如 dadb）；GitHub Actions 编译 artifact；推送后用 `scripts/fetch-apk.sh` 自动下载解压到 `/sdcard/Download/`，用户直接装 APK。不要 Release 发版那套

## 你的任务

（用户在此补充：例如「实现连接页」「修安装失败文案」「改 Actions」等）

---
