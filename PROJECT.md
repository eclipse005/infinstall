# 无限安装 / Infinstall

工程目录名：`infinstall`（本仓库路径）

## 产品

| 项 | 内容 |
|----|------|
| 中文名 | **无限安装**（谐音：无线安装；含义：少限制地装应用） |
| 英文名 | **Infinstall**（Infinite + Install） |
| 定位 | 手机端「无线给智能电视 / 机顶盒安装与管理应用」 |
| 不是 | 通用 ADB 调试器、命令行工具、极客终端 |

用户价值：没有 U 盘时，用手机把 APK 装到已开启**网络调试**的智能电视 / 机顶盒上，并可查看、卸载应用。

## 实现手段（用户无感知）

底层用网络调试协议与电视通信。  
**不出现在产品名、主界面主文案里。** 用户流程：**连电视 → 选包安装 → 管理应用**。

## 连接方式（电视优先）

**主路径（电视 / 机顶盒）：** 输入 **IP + 端口**（多为 `5555`）→ 连接。多数电视只要开「网络调试」，**不需要配对**。

**次路径（少见）：** 部分手机/平板「无线调试」需 **配对码** 后再连接。折叠进「连不上？使用配对码」，不当默认主入口。

**明确不做：**

- 局域网自动扫描  
- **二维码配对**（电视/盒子无摄像头，不适用主场景）  
- 产品名或主 UI 强调「ADB」  
- 「直接连接」等开发者黑话（统一叫「连接」）

## 完整功能

1. **连接**：IP 连接 + 历史；配对码为次要；断开检测；失败中文说明  
2. **安装**：选 APK 直接安装到设备（推送到临时目录后 `pm install`）  
3. **文件管理（做精）**：浏览/排序、快捷目录、上传/下载、新建文件夹、重命名、复制/剪切/粘贴、删除、属性、远程安装 APK  
4. **体验**：中文、字号偏大；手机 + 平板适配  

不再做「远程应用管理/卸载列表」主功能。

## 技术

Kotlin · Compose · [libadb-android](https://github.com/MuntashirAkon/libadb-android) · GitHub Actions 编 APK · `scripts/fetch-apk.sh`
