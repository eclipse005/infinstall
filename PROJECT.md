# 无限安装 / Infinstall

工程目录名：`infinstall`（本仓库路径）

## 产品

| 项 | 内容 |
|----|------|
| 中文名 | **无限安装**（谐音：无线安装；含义：少限制地装应用） |
| 英文名 | **Infinstall**（Infinite + Install） |
| 定位 | 手机端「无线给智能电视 / 机顶盒安装与管理应用」 |
| 不是 | 通用 ADB 调试器、命令行工具、极客终端 |

用户价值：没有 U 盘时，用手机把 APK 装到已开启**网络调试**的智能电视 / 机顶盒上，并可查看、卸载应用。

## 实现手段（用户无感知）

底层用 **网络调试（ADB over TCP/IP）** 与电视通信。  
电视开启开发者选项中的网络调试后，用户在电视上查看本机 IP，在本 App 中**手动输入 IP + 端口**连接。

**网络调试 / ADB 只是实现管道**，不出现在产品名、主界面主文案里。用户流程是：**连电视 → 选包安装 → 管理应用**。

## 连接方式

电视需先开启 **网络调试 / 网络 ADB**（开发者选项里）。手机与电视同一局域网。  
**不做二维码扫码**（电视不会为此提供连接二维码）。

连接优先级：

1. **自动发现（优先）**：在局域网扫描可能已开启网络调试的设备（常见端口如 `5555`），列出可点选连接
2. **连接历史**：最近成功连过的地址，一点即连
3. **手动输入 IP + 端口**（默认 `5555`）：扫描不到、多网段、电视未响应时的可靠后备——与常见 ADB 类工具相同

连上后若电视弹出「是否允许调试」，在电视上点允许。

## 完整功能范围

一次性做完整产品，不按「一期 / 二期」砍半交付：

1. **连接电视**
   - 局域网自动发现 + 连接历史 + 手动 IP/端口
   - 连接状态；失败时中文说明（未开调试、不同网段、电视上未点允许、端口错误等）
2. **安装 APK**
   - 系统文件选择器选 APK
   - 支持「分享到本 App」传入 APK
   - 传输与安装进度；成功 / 失败用人话提示
   - 支持一次选多个 APK 依次安装（批量）
3. **应用管理**
   - 列出电视上第三方应用
   - 确认后卸载
4. **体验与说明**
   - 连接页简要「如何开网络调试」说明
   - 界面中文；字号偏大，方便在客厅场景阅读
5. **手机 + 平板双端适配**（控制端：手机和平板都要舒服）
   - **手机**：单栏主流程，拇指区好点；表单与列表不挤、不处处横滑
   - **平板**：加宽布局——可用双栏（如左：连接/设备，右：安装或应用列表）、内容限最大宽度居中，避免一行字拉满整屏
   - **共用**：触控目标够大、间距宽松、横竖屏都能用；横屏平板不「又扁又挤」
   - 不单独做 iPad/Web；就是同一套 Android App，按屏幕宽度自适应

## 明确不做

- 二维码扫码连接
- 把通用 shell / 远程文件浏览器做成主功能
- Root
- 产品名或主 UI 强调「ADB」

## 技术方向

- Android App，**Kotlin**
- 系统文件选择器（SAF）选 APK
- 纯库实现调试协议（如 [dadb](https://github.com/mobile-dev-inc/dadb)），不内嵌完整 adb 二进制
- 无需 Root；需网络权限
- 编译：**GitHub Actions** 产出 APK artifact；本机用 `scripts/fetch-apk.sh`（`gh run download`）自动下载并解压到 `/sdcard/Download/`，用户直接安装。不搞 Release 发版流程

## 参考体验

连接方式参考常见工具：电视开网络调试 → 看 IP → App 填 IP。  
产品包装为**无线安装管理器**，而不是 ADB 调试器。

## 设备与环境

- 开发：可在平板 / Debian 等环境写代码，用 GitHub Actions 编译
- 安装端：智能电视 / TV Box / 机顶盒（已开网络调试）
- 控制端：安卓**手机与平板**（同一 App，双端适配；开发与自测会在平板上使用）
