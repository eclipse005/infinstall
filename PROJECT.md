# 无限安装 / Infinstall

工程目录名：`infinstall`（本仓库路径）

## 产品

| 项 | 内容 |
|----|------|
| 中文名 | **无限安装**（谐音：无线安装；含义：少限制地装应用） |
| 英文名 | **Infinstall**（Infinite + Install） |
| 定位 | 手机端「无线给智能电视 / 机顶盒 / 另一台安卓设备安装与管理应用」 |
| 不是 | 通用 ADB 调试器、命令行工具、极客终端 |

用户价值：没有 U 盘时，用手机把 APK 装到已开启网络调试 / 无线调试的设备上，并可查看、卸载应用。

## 实现手段（用户无感知）

底层用网络调试协议与目标设备通信。  
**不出现在产品名、主界面主文案里。** 用户流程：**连设备 → 选包安装 → 管理应用**。

## 连接方式（两种，无自动扫描）

**不做局域网自动扫描**（易误报、干扰使用）。  
**不做二维码扫码连接。**

### 1. 直接连接

- 手动输入 **IP + 端口** 后连接  
- 经典「网络调试」端口多为 `5555`  
- Android 11+「无线调试」在**配对成功后**，使用调试页上的 **连接端口**（与配对端口不同）  
- 连接历史：一点再连  

### 2. 配对设备（Android 11+ 无线调试）

与常见 ADB 助手一致：

1. 目标设备：开发者选项 → **无线调试** → **使用配对码配对设备**  
2. 在本 App 填入：**IP、配对端口、配对码** → 配对  
3. 配对成功后，切到「直接连接」，填无线调试主界面上的 **IP + 连接端口** → 连接  

## 完整功能范围

1. **连接**
   - 直接连接（IP + 端口 + 历史）
   - 配对设备（配对码 + IP + 配对端口）
   - 连接状态；失败中文说明  
2. **安装 APK**
   - 系统文件选择器 / 分享进入 / 批量  
   - 进度与结果人话提示  
3. **应用管理**
   - 第三方应用列表、确认卸载  
4. **体验**
   - 中文、偏大字号；手机单栏、平板宽屏适配  
   - 连接页简要开启说明  

## 明确不做

- 局域网自动扫描设备列表  
- 二维码扫码连接  
- 通用 shell / 远程文件浏览器作主功能  
- Root  
- 产品名或主 UI 强调「ADB」  

## 技术方向

- Kotlin Android App  
- SAF 选 APK  
- [libadb-android](https://github.com/MuntashirAkon/libadb-android)（含配对 + 连接）+ Conscrypt  
- GitHub Actions 编 debug APK；`scripts/fetch-apk.sh` 拉到 `/sdcard/Download/`  

## 自测建议

可用手机装本 App，平板开无线调试：先配对再连接，测安装/卸载（模拟电视）。  
